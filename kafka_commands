## Start Kafka server
bin/kafka-server-start.sh config/server.properties

## Create Topic
bin/kafka-topics.sh --create --zookeeper localhost:2181 --replication-factor 1 --partitions 1 --topic kafka-whatsapp

## Create Producer
bin/kafka-console-producer.sh --broker-list localhost:9092 --topic kafka-whatsapp

## Consumer
bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic kafka-whatsapp --from-beginning